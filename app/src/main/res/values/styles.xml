<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lightGrey</item>
        <item name="colorPrimaryDark">@color/heavyGrey</item>
        <item name="colorAccent">@color/skyBlue</item>
        <item name="searchViewStyle">@style/SearchViewTheme3</item>
    </style>

    <style name="Theme.AppCompat.Translucent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppToolbar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="MyThemeOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="searchViewStyle">@style/SearchViewTheme3</item>
    </style>

    <!--<style name="SearchViewTheme3" parent="Widget.AppCompat.SearchView">-->
        <!--<item name="searchIcon">@android:color/transparent</item>-->
        <!--<item name="searchHintIcon">@android:color/transparent</item>-->
        <!--<item name="queryBackground">@android:color/transparent</item>-->
        <!--<item name="submitBackground">@android:color/transparent</item>-->
    <!--</style>-->

    <style name="SearchViewTheme3" parent="Widget.AppCompat.SearchView">
        <item name="searchIcon">@null</item>
        <item name="searchHintIcon">@null</item>
        <item name="queryBackground">@android:color/transparent</item>
        <item name="submitBackground">@android:color/transparent</item>
    </style>

    <style name="MySeekBarStyle" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressBackgroundTint">@color/colorAccent</item>
        <item name="android:progressTint">@android:color/transparent</item>
        <item name="android:thumbTint">@color/colorPrimaryDark</item>
    </style>

    <style name="alertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
    </style>

</resources>
